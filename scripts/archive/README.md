# Archived one-shot scripts

These are historical codemods / manual test snippets run against the working tree
before the backend refactor. Kept for reference only. Already applied:

- `fix_cron.cjs`, `patch_sidebar.cjs`, `patch_filegallery*.cjs` — applied.
- `fix_all_serialized.cjs` + `fix_syntax.cjs` — applied then undone by `revert_serialized.cjs`.

None represent pending fixes. Safe to delete.
